export default function AdminPage() {
  return (
    <main className="mx-auto flex min-h-screen w-full max-w-5xl flex-col gap-4 px-6 py-20">
      <h1 className="text-3xl font-semibold tracking-tight text-stone-950">
        Admin Foundation Ready
      </h1>
      <p className="max-w-3xl text-stone-600">
        Protected admin routes, server actions, and content management query
        scaffolding are in place for the next implementation steps.
      </p>
    </main>
  );
}
